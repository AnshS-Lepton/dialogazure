@{
    Layout = "~/Views/Shared/_DashboardLayout.cshtml";
}
<!--@using SmartInventory.Controllers
@using SmartInventory.Helper
@model List<Models.Dashboard.Kpi_Master_View>
<div class="update_panel">

    <div id="" class="title d-flex">
        <h4>KPI List</h4>
        <a href="@Url.Action("Index", "Home", new { id = "" })" class="ml-auto kpiBtn">Add KPI</a>
    </div>
    <div class="pad_left">

        @(Html.Grid(Model).Id("grdMessage")
        .Build(columns =>
        {
        columns.Add().RenderedAs(model=> $"<a href='{@Url.Action("ViewKPICategoryUpdateMode", "Home",new{model.kpi_id})}' id='{model.kpi_id}'><img src='/images/edit.svg' class='edDel'/></a> <a href='javascript:void(0)' onclick='fnDeleteKPIName({model.kpi_id})' id='{model.kpi_id}'><img src='/images/delete.svg' class='edDel ml-03'/></a> <a href='javascript:void(0)' data_kpi_id='{model.kpi_id}' onclick=fnDownloadKPIExcel('{model.kpi_source}','{model.kpi_name.Replace(" ","_")}')><img src='/images/excel.svg' /></a>").Titled("Action").Encoded(false);

        columns.Add(model=> model.category_name).Titled("Category Name");
        columns.Add(model => model.kpi_name).Titled("KPI Name");-->
                    @*	columns.Add(model => model.kpi_source).Titled("KPI Source");s
        columns.Add(model=> model.x_axis_column).Titled("X-Axis Column");
        columns.Add(model=> model.y_axis_columns).Titled("Y-Axis Columns");
                    columns.Add(model=> model.is_visible).Titled("Is Visible");*@


        <!--})
        .Using(GridFilterMode.Header)
        .Empty("No data found")
        .Filterable()
        .Sortable()
        .Pageable(pager =>
        {
        pager.PageSizes = new Dictionary<Int32, String> { { 0, "All" }, { 10, "10" }, { 25, "25" }, { 50, "50" }, { 100, "100" } };
        pager.ShowPageSizes = true;
        pager.PagesToDisplay = 5;
        pager.CurrentPage = 1;
        pager.RowsPerPage = 10;
        })
        )
    </div>
</div>

@section Scripts{
    <script>
        function fnDownloadKPIExcel(kpi_source,kpi_name) {
    window.location = `/kpi/ExportKPIExcel?kpi_source=${kpi_source}&kpi_name=${kpi_name}`;
}
        function fnDeleteKPIName(kpi_id) {
            if (confirm("Are you sure, you want to delete this record.") == true) {
                $.get("/Home/DeleteKPICategory", { kpi_id: kpi_id }, function() {
                 location.reload(true);
                });
            }
        }
    </script>
}-->
