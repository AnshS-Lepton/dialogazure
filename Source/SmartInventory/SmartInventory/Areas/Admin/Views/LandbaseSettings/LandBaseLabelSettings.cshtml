@model Models.Admin.Landbase_label_Settings

<!DOCTYPE html>
<div class="col-lg-12" id="dvPageMessage">
    @{Html.RenderPartial("_PageMessage", Model.pageMsg);}
</div>
<div id="dvInfoSetting" class="update_panel">
    <div class="title">
        <h4><span class="icon16 icomoon-icon-bars"></span><span>Label Settings</span></h4>
    </div>
    @using (Html.BeginForm("SaveLandBaseLabelSettings", "LandbaseSettings", FormMethod.Post, new { autocomplete = "off", @id = "frmLandBaseLabelSetting", enctype = "multipart/form-data", @class = "form-horizontal  pad_left" }))
    {
        <div class="form-group">
            <div class="row">
                <div class="col-md-4">
                    <label class="control-label">
                        Layer Type:
                    </label>
                </div>
                <div class="col-md-8">
                    @Html.DropDownListFor(m => m.landbase_layer_id, new SelectList(Model.lstLayers.Select(x => new { Value = x.id.ToString(), Text = x.layer_name.ToString() }), "Value", "Text"), "--Select--", new { @class = "chosen-select col-sm-12", @id = "ddllstLayers", @onchange = "onChangeLandBaseAttributeBind();" })
                </div>
            </div>
        </div>
      
            <div class="form-group">
                <div class="row">
                    <div class="col-md-4">
                        <label class="control-label" style="float:left;"> Label Attributes:</label>
                    </div>

                    <div class="col-md-8"> 

                        <select id="ddlLandBaseAttributesDetails" name="@Html.NameFor(m => m.label_columns)" class="chosen-select col-sm-12">
                            <option value="">--Select--</option>
                            @foreach (var attribute in Model.lstLabelAttributes)
                            {
                                var selected = attribute.column_name == Model.label_columns ? "selected" : "";
                                <option value="@attribute.column_name" data-selectedvalue="@attribute.is_selected" @selected>@attribute.display_name</option>
                                
                            }
                        </select>

                    </div>

                </div>
            </div>
            <div class="form-group" id="dvBtnAction">
                <div class="row">
                    <div class="col-md-4"> </div>
                    <div class="col-md-8  text-right">
                        <input type="submit" class="btn btn-primary" onclick="return IsValiddropdown();" name="btnaction" id="btnSubmit" value="Save" />
                        <a href="@Url.Action("LandbaselabelSettings", "LandbaseSettings", new { id = 0 })" class="btn btn-default" title="Reset">Reset</a>
                    </div>
                </div>
            </div>
       
        <div class="clearfix"></div>
    }
</div>
<script>
    $(document).ready(function () {

        $('.chosen-select').chosen({ width: '100%' });
 
    });

    function IsValiddropdown() {

        if ($('#ddllstLayers').val() == "") {
            $("#ddllstLayers_chosen").css({ "width": "100% !important", "border": "1px solid red" });
            $("#ddlLandBaseAttributesDetails_chosen").css({ "width": "100% !important", "border": "0px" });
            return false;
        }

        if ($('#ddlLandBaseAttributesDetails').val() == "")
        {
            $("#ddlLandBaseAttributesDetails_chosen").css({ "width": "100% !important", "border": "1px solid red" });
            $("#ddllstLayers_chosen").css({ "width": "100% !important", "border": "0px" });
            return false;
        }

    }
   

</script>
