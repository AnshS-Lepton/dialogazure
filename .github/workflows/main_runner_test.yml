# author: hari-lepton
# date: 15-Jul-2024
name: Build and deploy on PR merge

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:
    branches: [ develop ]
  #pull_request:
    #branches: [ develop ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # Self hosted runner
    runs-on: [Product_SIT] #[windows-2019]

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v1.1
      
      - name: Setup NuGet
        uses: NuGet/setup-nuget@v1.0.5
      
      - name: Restore NuGet packages
        run: nuget restore Source/SmartInventory/SmartInventory.sln
        
      - name: Build the solution
        run: msbuild Source/SmartInventory/SmartInventory.sln /p:Configuration=Release

      - name: Deploy to Server Directory
        run: |
          BUILD_OUTPUT_DIR="Source/SmartInventory/SmartInventory/bin/Release"
          DEPLOY_DIR="D:/hari/runner_test"
          
          # Create the deployment directory if it doesn't exist
          mkdir -p $DEPLOY_DIR
          
          # Copy the build output to the deployment directory
          cp -r $BUILD_OUTPUT_DIR/* $DEPLOY_DIR/

